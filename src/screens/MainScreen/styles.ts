import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF',
    elevation: 1,
  },
  scrollViewContainer: {
    paddingBottom: 125,
  },
});
