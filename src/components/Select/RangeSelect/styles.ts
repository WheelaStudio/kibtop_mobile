import {StyleSheet} from 'react-native'

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    paddingHorizontal: 7,
    justifyContent: 'space-between',
    alignItems: 'center',
    height: 45,
    marginBottom: 15,
  },
  divider: {
    height: 45,
    width: 18,
  },
})
